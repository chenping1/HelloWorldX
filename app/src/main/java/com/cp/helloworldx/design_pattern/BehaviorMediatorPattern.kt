package com.cp.helloworldx.design_pattern

/**
 * 中介者模式
 * 说明: 定义一个单独的中介者对象，来封装一组对象之间的交互，避免对象之间的直接交互
 * 场景: 使 各个 对象不需要显示的相互引用， 从而使其耦合松散
 * 解决对象和对象指尖耦合关系
 *
 * MVP 的P, MVC 中的C, MVVM的VM
 *
 */
public class BehaviorMediatorPattern {

}